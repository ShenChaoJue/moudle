<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ziwen.moudle.mapper.rbac.UserRoleMapper">

    <!-- 结果集映射 -->
    <resultMap id="BaseResultMap" type="com.ziwen.moudle.entity.rbac.UserRole">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础列名 -->
    <sql id="Base_Column_List">
        id, user_id, role_id, create_time, update_time, is_deleted
    </sql>

    <!-- 删除用户角色关联（软删除） -->
    <update id="deleteUserRoles">
        UPDATE sys_user_role
        <set>
            is_deleted = 1,
            update_time = NOW()
        </set>
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND is_deleted = 0
    </update>

    <!-- 批量删除用户角色关联（软删除） -->
    <update id="deleteUserRolesBatch">
        UPDATE sys_user_role
        <set>
            is_deleted = 1,
            update_time = NOW()
        </set>
        WHERE user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
        AND is_deleted = 0
    </update>

    <!-- 批量插入用户角色关联 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO sys_user_role
        (user_id, role_id, create_time, update_time, is_deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.userId,jdbcType=BIGINT},
                #{item.roleId,jdbcType=BIGINT},
                #{item.createTime,jdbcType=TIMESTAMP},
                #{item.updateTime,jdbcType=TIMESTAMP},
                #{item.isDeleted,jdbcType=INTEGER}
            )
        </foreach>
    </insert>

</mapper>
